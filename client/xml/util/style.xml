<f:includes
	xmlns:f="http://www.worldmerge.com/2008/xsl"
	xmlns:xsl="http://www.w3.org/1999/XSL/Transform"
	xmlns:html="http://www.w3.org/1999/xhtml"
	xmlns:svg="http://www.w3.org/2000/svg">

	<xsl:template name="style">
		<xsl:param name="position" select="'absolute'" />
		<xsl:param name="width" />
		<xsl:param name="height" />
		<xsl:param name="left"/>
		<xsl:param name="top" />
		<xsl:param name="right" />
		<xsl:param name="bottom" />
	
		<xsl:attribute name="style">
			position: <xsl:value-of select="$position" />;
			width: <xsl:value-of select="$width" />px;
			height: <xsl:value-of select="$height" />px;
			left: <xsl:value-of select="$left" />px;
			top: <xsl:value-of select="$top" />px;
			right: <xsl:value-of select="$right" />px;
			bottom: <xsl:value-of select="$bottom" />px;
		</xsl:attribute>
	</xsl:template>
</f:includes>