template () {
	divide = function (x, y) {
		return x / y;
	},
	swapDivide = function (x, y) {
		return y / x;
	},
	multiply = function (x, y) {
		return x * y;
	},
	min = function (x, y) {
		return Math.min(x, y);
	},
	max = function (x, y) {
		return Math.max(x, y);
	},
	abs = function (x) {
		return Math.abs(x);
	},
	<f:call>content</f:call>
}